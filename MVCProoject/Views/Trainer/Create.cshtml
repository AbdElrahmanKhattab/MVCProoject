@model TrainerFormViewModel

@{
    ViewData["Title"] = "Create Trainer";
}

<section class="my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <p class="text-primary-color fw-bold mb-1">Trainer Module</p>
            <h1 class="fw-bolder text-black-alternative">Create Trainer</h1>
        </div>
        <a asp-action="Index" class="btn btn-outline-secondary rounded-pill">Back</a>
    </div>

    <partial name="_TrainerForm" model="Model" />
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
