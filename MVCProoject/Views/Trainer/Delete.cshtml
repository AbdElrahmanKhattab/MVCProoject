@model Trainer

@{
    ViewData["Title"] = "Delete Trainer";
}

<section class="my-5">
    <div class="bg-white border rounded-4 p-4 shadow-sm">
        <p class="text-danger fw-bold mb-1">Delete Trainer</p>
        <h1 class="fw-bolder text-black-alternative">Delete @Model.Name?</h1>
        <p class="text-muted">This will permanently remove this trainer.</p>

        <dl class="row">
            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9">@Model.Email</dd>
            <dt class="col-sm-3">Specialty</dt>
            <dd class="col-sm-9">@Model.Specialty</dd>
        </dl>

        <form asp-action="Delete" asp-route-id="@Model.Id" method="post" class="d-flex gap-2">
            <button type="submit" class="btn btn-danger rounded-pill px-4">Delete</button>
            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
        </form>
    </div>
</section>
